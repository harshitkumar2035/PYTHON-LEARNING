# Replace the double space frome problem 3 with single spaces.

name = ("John is a good boy and")

print(name.replace(" ", "  ")  )
print(name) # Strings are immutble Which means that you cannot change them by running functions on them